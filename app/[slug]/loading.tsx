import { RouteLoadingFallback } from '@/app/_components/RouteLoadingFallback'

/** Cardápio público e sub-rotas por slug. */
export default function StorefrontSegmentLoading() {
  return <RouteLoadingFallback className="bg-[#faf8f6]" />
}
